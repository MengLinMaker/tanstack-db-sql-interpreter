{
  "name": "@menglinmaker/sql-parser-sample",
  "private": true,
  "type": "module",
  "scripts": {
    "test:lint": "biome lint --write",
    "test:type": "tsgo --ignoreConfig --noEmit --allowImportingTsExtensions --module esnext ./**/*.ts",
    "test:unit": "vitest run --coverage",
    "build": "rm -rf ./dist && mkdir ./dist && lezer-generator ./src/grammar/sql.grammar -o ./dist/parser.js"
  },
  "devDependencies": {
    "@lezer/generator": "^1.8.0"
  },
  "dependencies": {
    "@lezer/common": "^1.5.1",
    "@lezer/lr": "^1.4.8"
  }
}
